<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" />
    <stroke
        android:width="2dp"
        android:color="@color/blue" /> <!-- Change stroke color to blue (#0000FF) -->
    <corners android:radius="12dp" />
</shape>